killFeed.ranks = { // Which ranks can see the kill feed.
	"superadmin",
	"admin"
};

killFeed.chatSee = { // Who can see chat death messages.
	"superadmin",
	"admin"
};

killFeed.jobs = { // Which jobs can see the kill feed. (Uses Darkrp)
	TEAM_ADMIN
};

killFeed.showAdminDeaths = true; // Should the kill feed show admins death? (true or false)
killFeed.showChatMessages = true; // Should deaths show in chat?